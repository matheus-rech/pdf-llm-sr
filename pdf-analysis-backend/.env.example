# Gemini API Configuration
GEMINI_API_KEY=your_gemini_api_key_here

# PDF Text Extraction Configuration
# Set to 'true' to enable enhanced extraction with NanoNets docext (requires GPU for optimal performance)
# Set to 'false' to use PyPDF2 for basic text extraction (faster, no GPU required)
USE_NANONETS_EXTRACTION=false
